<div class="contents row">
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @tweets.each do |tweet| %>
    <div class="content_post" >
      <p><%= tweet.title %></p>
      <span class="name"><%= tweet.text %></span>
    </div>
  <% end %>
</div>